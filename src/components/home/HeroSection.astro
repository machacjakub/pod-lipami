---
---
<section id="hero-wrapper">
    <div id="hero">
        <div id="blurred-line"/>
        <div>
            <h1>Fyzioterapie Pod Lipami</h1>
            <h4>Mladá Boleslav</h4>
            <a href="#kontakt"><button client:only="react" id="reservation-button">Rezervovat termín</button></a>
        </div>
        <img id="hero-image-desktop" alt="Fyzioterapie pod lipami - úvodní obrázek" src="/hero-img.png" />
    </div>
    <img id="hero-image-phone" alt="Hero image" src="/hero-img.png" />
</section>
<style>
    h1 {
        color: var(--green-dark);
        font-size: 48px;
        margin-bottom: 16px;
        margin-right: max(20px, calc(60vw - 800px));
    }
    h4 {
        color: var(--green-light);
        font-size: 28px;
        font-weight: normal;
        margin: 16px auto 32px auto;
    }
    button {
        background-color: var(--salmon-light);
        padding: 15px 24px;
        font-family: Sofia Sans, sans-serif;
        border: none;
        border-radius: 18px;
        font-size: 22px;
        font-weight: 600;
        box-shadow: 4px 4px 8px rgba(0, 0, 0, 0.34);
        cursor: pointer;
        color: var(--salmon-darker);
    }
    button:hover {
        box-shadow: 0 0 10px white;
    }
    #hero-wrapper {
        height: 90vh;
        display: flex;
        align-items: center;
    }
    #hero {
        display: flex;
        width: calc(100vw - 50px);
        justify-content: space-between;
        align-items: center;
        margin: 60px 0 30px 0;
        background-image: linear-gradient(white, var(--background-light-blue), var(--background-light-blue), var(--background-light-blue), var(--background-light-blue), var(--background-light-blue), var(--background-light-blue), white);
        padding: 30px 0 30px 50px;
    }
    #hero-image-desktop {
        height: 400px;
        border-radius: 20px 0 0 20px ;
        border: var(--green-light ) solid;
        border-width: 10px 0 10px 10px;
    }
    #hero-image-phone {
        display: none;
    }
    #blurred-line {
        width: 20px;
        height: 420px;
        margin-right: 40px;
        background-color: var(--green-light);
        filter: blur(5px);
        flex-shrink: 0;
    }
    #services {
        height: 600px;
        background-color: darkgreen;
        width: 100vw;
    }
    a {
        all: unset;
    }


    @media (max-width: 1150px) {
        #hero-wrapper {
            margin-top: 40px;
            display: flex;
            flex-direction: column;
            align-items: initial;
            height: fit-content;
        }
        #hero-image-desktop {
            display: none;
        }
        #hero-image-phone {
            display: initial;
            margin: auto;
            width: 90vw;
            border-radius: 20px;
            border: 5px var(--green-light ) solid;
        }
        h1 {
            font-size: 38px;
            margin-bottom: 10px;
        }
        h4 {
            font-size: 22px;
            margin: 10px auto 20px auto;
        }
        button {
            padding: 12px 18px;
            font-size: 20px;
        }
        #hero {
            gap: 20px;
            padding: 30px 10% 30px 5%;
            justify-content: left;
            width: calc(100vw - 15%);
        }
        #blurred-line {
            width: calc(10px + 1%);
            height: 360px;
            margin-right: 10%;
            flex-shrink: 0;
        }
        @media (max-width: 400px) {
            h1 {
                font-size: 30px;
            }
            h4 {
                font-size: 18px;
            }
            button {
                padding: 10px 16px;
                font-size: 18px;
                border-radius: 10px;
            }
        }
    }
</style>